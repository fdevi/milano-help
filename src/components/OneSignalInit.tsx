/**
 * OneSignal is now initialized directly in index.html (required for iOS Safari/PWA).
 * This component is kept as a no-op for backward compatibility.
 */
const OneSignalInit = () => null;

export default OneSignalInit;
